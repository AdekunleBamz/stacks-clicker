# Offline click feedback

Add an offline-browser pass to release smoke notes so click attempts fail with clear local feedback instead of looking like a stalled wallet request.

## Checklist

- Disable network access before one click attempt.
- Confirm the retry path is visible after reconnecting.
