# Onboarding resume step storage

Track a follow-up for confirming onboarding resumes at the last incomplete step after refresh.

The stored step should be scoped to the browser session and should not depend on wallet address.
