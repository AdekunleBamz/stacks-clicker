# Support bundle session id

Track a support-bundle convention for including a short random session id with error screenshots.

The id should help correlate logs without exposing the connected wallet address.
