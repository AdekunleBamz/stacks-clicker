# Leaderboard empty network state

Add an empty-state check for the leaderboard when the selected network has no indexed players yet.

The copy should make it clear that the network is empty, not that the wallet connection failed.
