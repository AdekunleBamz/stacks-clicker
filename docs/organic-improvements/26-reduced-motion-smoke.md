# Reduced-motion smoke check

Verify motion-heavy components remain usable when reduced-motion is enabled.
Animation fallback paths should preserve timing cues and hierarchy.

- Include reduced-motion checks on both desktop and mobile browsers.

### Follow-up
- Re-run reduced-motion checks when introducing new animations.
- Confirm reduced-motion mode still exposes progress and success cues clearly.
