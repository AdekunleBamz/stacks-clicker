# Deployment env snapshot

Keep a deployment environment snapshot with release evidence so contract names, network, and explorer base URLs can be audited later.

## Checklist

- Record the sanitized frontend environment values used for the release.
- Store the snapshot beside the release note or deployment checklist.
