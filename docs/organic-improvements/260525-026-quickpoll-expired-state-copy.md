# QuickPoll expired state copy

Document copy expectations for polls that expire while the user is reading the options.

The state should explain that voting closed and preserve the visible results when available.
