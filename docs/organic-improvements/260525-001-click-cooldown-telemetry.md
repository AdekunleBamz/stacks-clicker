# Click cooldown telemetry sampling

Track click cooldown events as aggregate counts instead of wallet-scoped rows.

This keeps product telemetry useful for tuning the cooldown window without adding identity risk to release exports.
