# Language toggle native name

Add a localization review item for showing each language option in its own display name.

This keeps the toggle usable when the current locale is unfamiliar to the user.
