# Click burst feedback copy

Clarify the text shown when several clicks are queued before the wallet or network state catches up.

- Keep the message short enough for mobile toast layouts.
- Distinguish a local click burst from an on-chain write that is still pending.
- Avoid promising that score changes are final until the transaction state is known.
