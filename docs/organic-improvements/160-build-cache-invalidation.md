# Build cache invalidation

For dependency or Vite config changes, note whether the preview cache should be
cleared before validation. This keeps stale bundles from masking production
build regressions.
