# Contract read fallback message

Track a copy check for read-only contract failures that still leave cached app state visible.

Fallback copy should say which data may be stale and suggest a safe retry.
