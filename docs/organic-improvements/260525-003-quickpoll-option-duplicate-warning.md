# QuickPoll duplicate option warning

Record a UX follow-up to warn creators when two poll options normalize to the same visible label.

The note keeps the duplicate-option case separate from contract submission errors during triage.
