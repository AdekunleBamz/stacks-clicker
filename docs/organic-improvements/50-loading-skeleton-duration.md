# Loading skeleton duration check

Review skeleton display timing against real network behavior.
Skeletons that disappear too early can cause visual flicker and distrust.

- Reconfirm skeleton timeout assumptions when loading placeholders are redesigned.

### Follow-up
- Recheck skeleton timing targets when loading behavior is adjusted.
- Verify skeleton removal is tied to data readiness, not fixed delay-only timers.
