# Wallet provider version note

Track wallet extension versions used during regression runs.
Version drift is a common source of inconsistent connect behavior.

- Validate provider version notes against latest release channels each quarter.

### Follow-up
- Capture provider version changes in release notes whenever wallet deps bump.
- Record browser and OS details with provider version notes for reproducibility.
