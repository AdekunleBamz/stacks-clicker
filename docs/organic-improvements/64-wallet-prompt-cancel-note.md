# Wallet prompt cancel note

Keep the cancel path visible in wallet prompt smoke checks so rejected click, tip, and poll actions return users to the same screen without losing draft input.

## Checklist

- Cancel one wallet prompt from each write flow during release smoke testing.
- Confirm the visible error copy distinguishes user cancellation from network failure.
