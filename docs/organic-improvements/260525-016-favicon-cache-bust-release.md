# Favicon cache bust release note

Record a release check for verifying favicon updates are visible after a clean browser cache.

The check is useful because wallet-extension previews can hold older app metadata longer than the page shell.
