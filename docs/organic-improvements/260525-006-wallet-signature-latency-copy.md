# Wallet signature latency copy

Track whether signing-pending copy distinguishes wallet confirmation time from app processing time.

That distinction helps support avoid sending users through reconnect steps while a wallet prompt is still open.
