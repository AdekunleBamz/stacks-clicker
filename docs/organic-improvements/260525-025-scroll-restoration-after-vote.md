# Scroll restoration after vote

Add a mobile QA check for returning focus and scroll position after a QuickPoll vote completes.

The result state should appear near the option the user selected rather than jumping to the page top.
