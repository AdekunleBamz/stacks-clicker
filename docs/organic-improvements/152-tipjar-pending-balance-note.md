# Tipjar pending balance note

Support handoffs should distinguish wallet balance, pending tip amount, and confirmed tip history.

If a transaction is still pending, include the explorer link and the last observed status instead of asking the user to retry immediately.

This keeps duplicate tips out of the release support loop.
