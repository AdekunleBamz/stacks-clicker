# Batch click feedback latency

Measure UI feedback delay after batched click submissions.
Users should receive immediate confirmation even before final chain settlement.

- Reconfirm latency targets when click animation timing is adjusted.

### Follow-up
- Re-run feedback latency checks after animation timing adjustments.
- Capture p95 feedback latency alongside averages to detect intermittent lag.
