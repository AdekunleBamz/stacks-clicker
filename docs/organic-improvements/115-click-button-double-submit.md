# Click Button Double Submit

- Disable duplicate click submits while a transaction is pending.
- Confirm rapid taps do not enqueue duplicate wallet prompts.
- Keep the pending label visible until the action resolves.
