# Rate limit error retry copy

Keep rate-limit messaging in QA notes so rapid click attempts explain when users should wait before retrying.

## Checklist

- Trigger the local rate-limit path with repeated clicks.
- Confirm the retry copy includes a clear wait cue instead of a generic failure.
