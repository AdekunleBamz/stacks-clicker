# Custom tip decimal rounding

Track a validation check for custom tips with more decimal places than the contract accepts.

The UI should explain any rounding or rejection before the wallet prompt opens.
