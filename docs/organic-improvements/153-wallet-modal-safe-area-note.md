# Wallet modal safe area note

Mobile smoke checks should open the wallet connection modal with the browser toolbar visible.

Confirm the primary connect action and close control remain reachable inside the safe area on iOS and Android viewports.

Record the viewport used when a wallet provider renders its own embedded sheet.
