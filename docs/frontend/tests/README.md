# Frontend Tests

This folder documents frontend unit/integration test intent for suites under `frontend/src/tests` and `frontend/src/__tests__`.
Update these notes whenever a suite gains new behavioral assertions.
Run the frontend suite with `npm --prefix frontend run test -- --run`.
From the repository root, `npm run frontend:test` is the equivalent helper command.

## Maintenance Note
- Keep flaky-test triage guidance updated as CI retry policy evolves.

Source directory: `frontend/src/tests/`.

- Keep this index in sync when adding new targeted frontend test docs.

- Keep this test-doc index aligned with any newly documented suites.

### Maintenance Note
- Keep this test index synchronized with newly added frontend test suites.
