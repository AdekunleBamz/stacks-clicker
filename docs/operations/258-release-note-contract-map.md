# Release note contract map

Release notes should list the contract address and network when a deploy touches contract-facing UI.

Use environment names only; do not paste local secrets or wallet files.
