# Clicker card empty balance note

Check the clicker card renders a stable empty-balance state before wallet connection.

This prevents wallet connection issues from being mistaken for layout regressions.
