# Sound Toggle Persistence

Maintenance check:
- Verify muted sound preferences persist across reloads and wallet reconnects.
- Note browser-specific storage exceptions in QA if persistence changes.

