# Pull to refresh throttle note

During mobile smoke checks, verify pull-to-refresh does not trigger duplicate contract reads.

Capture request timing only, not wallet secrets, when filing a regression.
