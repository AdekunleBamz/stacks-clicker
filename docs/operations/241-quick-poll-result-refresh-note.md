# Quick poll result refresh note

When testing quick polls, confirm the result count updates after both a successful vote and a page reload.

This keeps UI cache issues separate from contract read failures.
