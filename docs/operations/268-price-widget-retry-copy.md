# Price Widget Retry Copy

Maintenance check:
- Confirm stale price states use retry language instead of transaction failure language.
- Keep the support playbook updated with the current price API fallback behavior.

