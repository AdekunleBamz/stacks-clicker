# Branching Guide

Primary branch is `main`.

For risky work, develop in short-lived branches and merge after verification.
For urgent fixes, use focused hotfix commits with clear rollback paths.
Sync with latest `main` before opening a release-bound pull request.
